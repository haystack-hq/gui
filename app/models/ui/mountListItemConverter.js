class MountListItemConverter {
	constructor(mountList) {
		this.list = mountList;

		// loop mount list items
		// transform the data how we need using MountListItem
		// add to this.list
		// return this.list
	}
}
module.exports = MountListItemConverter;